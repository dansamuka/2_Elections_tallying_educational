# Malava source documents

The scheduled historical-form sync discovers and archives the IEBC-published Malava Form 35A files here (and under the public immutable form archive).

This directory starts empty deliberately. The first sync must discover all **198** individual Form 35A assignments before it creates the review-only stream skeleton. A partial portal response is rejected rather than silently becoming an incomplete benchmark.
